import 'package:flutter/material.dart';

class Style_Text extends StatelessWidget {
  Style_Text({super.key});

  @override
  Widget build(context) {
    return const Text(
      'Hello World !',
      style: TextStyle(color: Color(0xFFfdf2e9), fontSize: 30),
    );
  }
}
